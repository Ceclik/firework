using Utils;

public class CameraTexture : Singleton<CameraTexture>
{
    // Use this for initialization
    private void Start()
    {
    }

    // Update is called once per frame
    private void Update()
    {
    }
}